**1.2.3.** 개발자 인터랙션 명세  


**1.2.3.1.** 점수 계산 API  

1: 엔드포인트  
　A. POST /api/v1/partner/grade/calculate  

2: 요청 바디  

```json
{
  "reportRate": 95,
  "scanRate": 105,
  "useWithfomOrder": true
}
```

3: 응답 구조  

```json
{
  "calculation": {
    "reportRateScore": {
      "input": 95,
      "level": "중",
      "score": 60,
      "weighted": 27
    },
    "scanRateScore": {
      "input": 105,
      "level": "중",
      "score": 60,
      "weighted": 27
    },
    "withfomOrderScore": {
      "input": true,
      "score": 100,
      "weighted": 10
    },
    "totalScore": 64,
    "predictedGrade": "gold",
    "gradeInfo": {
      "name": "골드",
      "icon": "🥇",
      "color": "#FFD700"
    },
    "nextGrade": {
      "name": "플래티넘",
      "requiredScore": 73,
      "gap": 9,
      "requirements": [
        "보고율 100% & 실질 90% 이상 필요",
        "스캔율 110% 초과 필요",
        "위드폼 오더 이용 필수"
      ]
    }
  }
}
```


**1.2.3.2.** 클라이언트 측 점수 계산  

```javascript
const calculateGradeScore = (reportRate, scanRate, useWithfomOrder) => {
  // 보고율 점수 (45%)
  let reportScore = 0;
  if (reportRate < 85) reportScore = 20;
  else if (reportRate < 100) reportScore = 40;
  else reportScore = 60;
  // 1년 유지 시 80점 (플래티넘 조건)
  
  // 스캔율 점수 (45%)
  let scanScore = 0;
  if (scanRate < 80) scanScore = 20;
  else if (scanRate < 96) scanScore = 40;
  else if (scanRate <= 110) scanScore = 60;
  else scanScore = 80; // 110% 초과
  
  // 위드폼 오더 점수 (10%)
  const orderScore = useWithfomOrder ? 100 : 0;
  
  // 가중 합산
  const totalScore = 
    (reportScore * 0.45) + 
    (scanScore * 0.45) + 
    (orderScore * 0.10);
  
  // 등급 판정
  let grade = 'bronze';
  if (totalScore >= 73) grade = 'platinum';
  else if (totalScore >= 55) grade = 'gold';
  else if (totalScore >= 36) grade = 'silver';
  
  // 플래티넘은 위드폼 오더 필수
  if (grade === 'platinum' && !useWithfomOrder) {
    grade = 'gold';
  }
  
  return { totalScore, grade };
};
```


**1.2.3.3.** 컴포넌트 상태 관리  

1: 탭 상태 (activeTab)  
　A. bronze / silver / gold / platinum  
　B. 기본값: 현재 파트너 등급  

2: 계산기 상태 (calculatorState)  
　A. isOpen: boolean  
　B. reportRate: number (0-100)  
　C. scanRate: number (0-200)  
　D. useWithfomOrder: boolean  
　E. result: object (계산 결과)  

3: 팝업 상태 (popupState)  
　A. isOpen: boolean  
　B. resultData: object  


**1.2.3.4.** 접근성 요구사항  

1: 탭 영역 ARIA  
　A. role="tablist"  
　B. aria-label="등급별 기준표"  
　C. 각 탭: role="tab", aria-selected="true/false"  

2: 계산기 영역 ARIA  
　A. 슬라이더: aria-valuemin, aria-valuemax, aria-valuenow  
　B. 토글: role="switch", aria-checked  

3: 키보드 내비게이션  
　A. ← / →: 탭 간 이동  
　B. Enter / Space: 탭 선택  
　C. Tab: 계산기 필드 간 이동  


**1.2.3.5.** 에러 처리  

1: 에러 코드  
　A. GRD_003: 점수 계산 실패  

2: 사용자 메시지  
　A. "점수 계산에 실패했습니다"  
　B. 재시도 버튼 표시  


**1.2.3.6.** 성능 최적화  

1: 계산 최적화  
　A. debounce 적용 (300ms)  
　B. 클라이언트 측 계산 우선, 서버 검증 후처리  

2: 렌더링 최적화  
　A. 선택된 탭만 렌더링 (lazy rendering)  
　B. 탭 전환 시 이전 탭 상태 유지  


• 참조: 7-2-4-1 > 1.1.3. 개발자 인터랙션 명세
