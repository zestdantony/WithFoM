---
description: "BIZ docs constitution for WithFoM (applies only when MODE=BIZ)"
globs:
  - "**/*.md"
alwaysApply: false
---

# WF BIZ Docs Rules (MODE=BIZ only)

## 0) Activation
- Apply this rule ONLY when MODE=BIZ (set by WF Router).
- If MODE is DEV or CODE, ignore this entire file.

## 1) What this rule does
- Enforce the "문서 작성 헌법(Constitution)" for business/policy/proposal docs.
- Execute-first: when the user instructs to modify, do it and report the result (do not ask clarification).
- If something is impossible/ambiguous, mark "확인 필요" in the report ONLY.
- Goal: minimize errors and omissions. Never arbitrarily summarize, change meaning, or polish sentences in original content.

## 2) 핵심 원칙

### 2-1. 내용 보존(원문 100%)
- 문서의 원문 텍스트는 토시 하나도 변경/삭제/추가 금지 (오타 포함). 단, 줄끝 역슬래시(\) 제거는 표기 오류 복구로 허용.
- 허용되는 변경: 표기/계층/들여쓰기/줄바꿈(레이아웃) 및 강조(볼드) 표기만.
- 순서 변경 금지 (문단/항목 위치 이동 금지).
- 치환 토큰($1 등) 사용 금지.

### 2-2. 추측 금지
- 정보가 부족하면 추측하지 말고 "확인 필요"로 표기.
- 근거 없는 수정/지적 금지.

## 3) 계층(번호 체계) 규칙

### 3-1. 단계별 번호 형식
| 단계 | 형식 | 예시 |
|------|------|------|
| 1단계 (대챕터) | `# 1.` | `# 1. 대분류` |
| 2단계 (중챕터) | `## 1-1.` | `## 1-1. 주요 카테고리` |
| 3단계 (소챕터) | `1-1-1.` | `1-1-1. 소제목` (볼드) |
| 4단계 (세부챕터) | `1-1-1-1.` | `1-1-1-1. 세부 제목` (볼드) |
| 5단계 (주제) | `1.1.` | `**1.1.** 주제명` (볼드) |
| 6단계 (본문 항목) | `1.1.1.` | `**1.1.1.** 항목명` (볼드) |
| 7단계 (소항목) | `1.1.1.1.` | `**1.1.1.1.** 소항목명` (볼드) |
| 8단계 (상세 항목) | `1:` `2:` `3:` | `　1: 내용` |
| 9단계 (설명) | `A.` `B.` `C.` | `　　A. 내용` |
| 10단계 (설명) | `a.` `b.` `c.` | `　　　a. 내용` |
| 11단계 (상세) | `aa.` `bb.` `cc.` | `　　　　aa. 내용` |
| 12단계 (상세) | `aaa.` `bbb.` `ccc.` | `　　　　　aaa. 내용` |
| 13단계 (상세) | `•` | `　　　　　　• 내용` |

- 4단계까지만 하이픈 확장 허용. 1-1-1-1-1 이상은 금지.
- (1), (2), ①, ② 등 대체 번호 금지.

### 3-2. 들여쓰기 규칙 (전각 공백만 사용)
| 단계 | 들여쓰기 |
|------|----------|
| 1~7단계 | 들여쓰기 없음 (0) |
| 8단계 (1:) | 전각 공백 1개 |
| 9단계 (A.) | 전각 공백 2개 |
| 10단계 (a.) | 전각 공백 3개 |
| 11단계 (aa.) | 전각 공백 4개 |
| 12단계 (aaa.) | 전각 공백 5개 |
| 13단계 (•) | 전각 공백 6개 |

- 들여쓰기는 반드시 전각 공백(　)만 사용. 반각 스페이스/탭 금지.

### 3-3. 제목 스타일 규칙
- 1~2단계(#, ##)는 마크다운 제목 스타일로 표기.
- 3단계부터는 마크다운 제목 스타일 사용 안 함, 일반 텍스트로 표기.
- 3~7단계 라인은 "번호 + 제목" 전체를 **볼드**로 표기.
- 볼드는 번호부터 제목 끝까지만 적용 (들여쓰기는 볼드 처리하지 않음).

### 3-4. 단계 전환 시 줄 간격 규칙
- 하위 → 상위 전환 시: 빈 줄 2줄 (예: 5단계 다음에 2단계가 올 때).
- 같은 단계 또는 상위 → 하위 전환 시: 빈 줄 1줄.
- 8단계(1:) 항목과 항목 사이: 빈 줄 1줄.

## 4) 줄/공백 규칙

### 4-1. 줄바꿈 강제
- 모든 줄의 끝에 공백 2칸 입력 (제목/본문/번호/불릿 포함).
- 빈 줄도 예외 없이 공백 2칸만 있는 줄로 처리.

### 4-2. 줄바꿈(행갈이) 시 들여쓰기 규칙
- 한 항목 내용이 길어 줄바꿈할 때, 다음 줄은 같은 단계의 기본 들여쓰기만 적용.
- 번호/알파벳/불릿 길이만큼 추가 전각 공백으로 정렬하는 방식 금지.
- 줄바꿈한 다음 줄에는 번호/알파벳/불릿을 반복하지 않고 내용만 이어 씀.

## 5) 표기 방식 규칙

### 5-1. 마크다운 자동변환 무력화
- 8단계 본문 항목 번호는 반드시 `1:` 형태 사용 (`1.` 형태 금지).
- 불릿은 반드시 `•` 형태 사용 (`-`, `*` 형태 금지).

### 5-2. 부연설명 표기(↳) 규칙
- ↳는 설명(부연) 라인에만 사용. 번호(1:), 알파벳(A.), 불릿(•)의 대체로 사용 금지.
- ↳ 라인은 해당 내용이 설명하는 대상과 같은 단계의 기본 들여쓰기만 적용.
- ↳ 라인도 줄 끝 공백 2칸 규칙 동일 적용.

### 5-3. 강조(볼드) 표기 규칙
- 강조는 원칙적으로 텍스트 일부에만 사용. 3~7단계 라인은 "번호 + 제목" 전체 볼드.
- 강조 표기는 **텍스트** 형태만 허용.
- 강조는 번호(1:), 알파벳(A.), 불릿(•), ↳의 대체 표기로 사용 금지.
- 강조는 계층 구조를 바꾸지 않으며, 기존 문장/숫자/기호를 임의 수정하지 않음.

## 6) 금지 규칙

### 6-1. 줄끝 역슬래시(\) 금지
- 줄끝에 역슬래시(\) 사용 금지. 줄바꿈 강제는 "줄끝 공백 2칸"만 사용.
- 기존 문서 내 줄끝 역슬래시는 "표기 오류"로 간주하고 제거.

### 6-2. 기타 금지 사항
- (1), (2), ①, ② 등 대체 번호 금지.
- 1-1-1-1까지 허용, 그 이상(1-1-1-1-1 …) 하이픈 확장 번호 금지.
- 임의 요약/의미 변경/문장 다듬기 금지.

## 7) 작업 안전 규칙

### 7-1. 오류 방지 운영 규칙
- 일괄 편집 금지, 항상 구간 단위로 적용.
- 적용 전/후에 원문 ↔ 결과를 항목 단위로 대조해 누락/변형 없음을 확인.
- 오류 가능성이 있는 구간(들여쓰기/줄끝 공백)은 소구간 테스트 후 확장 적용.

### 7-2. 표기 매핑 재확인
- 작업 시 계층 표기 매핑을 항상 확인:
  - 3단계: 들여쓰기 없음 + 1-1-1. (볼드)
  - 4단계: 들여쓰기 없음 + 1-1-1-1. (볼드)
  - 5단계: 들여쓰기 없음 + 1.1. (볼드)
  - 6단계: 들여쓰기 없음 + 1.1.1. (볼드)
  - 7단계: 들여쓰기 없음 + 1.1.1.1. (볼드)
  - 8단계: 전각 1개 + 1:
  - 9단계: 전각 2개 + A.
  - 10단계: 전각 3개 + a.
  - 11단계: 전각 4개 + aa.
  - 12단계: 전각 5개 + aaa.
  - 13단계: 전각 6개 + •

## 8) 최종 점검 체크리스트

### 8-1. 표기 체크
- [ ] 모든 8단계 본문 항목 번호가 1: 형태로 되어 있는가
- [ ] 모든 불릿이 • 로 되어 있는가
- [ ] 들여쓰기가 전각 공백(　)으로만 되어 있는가
- [ ] 모든 줄 끝에 공백 2개가 존재하는가 (빈 줄 포함)
- [ ] 줄끝 역슬래시(\)가 존재하지 않는가
- [ ] 3~4단계 라인이 "번호 + 제목" 전체 볼드로 적용되었는가
- [ ] 5~7단계 라인이 "번호 + 제목" 전체 볼드로 적용되었는가
- [ ] 1~2단계가 마크다운 제목 스타일(#, ##)로 표기되었는가
- [ ] 8~13단계 들여쓰기가 단계별 전각 공백 개수에 맞는가
- [ ] 하위→상위 전환 시 빈 줄 2줄이 있는가
- [ ] ↳가 설명 라인으로만 사용되었는가

### 8-2. 내용 체크
- [ ] 원문 텍스트가 토시 하나도 변형되지 않았는가
- [ ] 순서가 변경되지 않았는가
- [ ] 누락된 항목이 없는가
- [ ] "확인 필요" 항목이 명확히 표기되었는가

## 9) Added Structure Rules (BIZ only)
- Overview numbering: use ".0" for section overview (e.g., 1.0. 섹션 개요).
- Long content handling:
  - If a section becomes long, convert that section into a folder.
  - Create the next level as files inside the folder.
- Same-level mixing ban:
  - Within one parent folder, do not mix files and folders for the same level.
- 5th-level numbering inheritance:
  - The first segment of 5th-level numbering must equal the last number of its 4th-level.
  - Example: under 7-2-2-3 → 3.0, 3.1, 3.2 ...
